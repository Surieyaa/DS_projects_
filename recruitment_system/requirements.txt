# CPU-only torch build — much smaller/faster to install than the default
# CUDA wheel, and this app never uses a GPU. Matters a lot on Render's
# build time & memory limits.
--extra-index-url https://download.pytorch.org/whl/cpu

Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Werkzeug==3.1.7
sentence-transformers>=3.0.0
torch>=2.2.0
PyMuPDF==1.28.0
python-docx==1.2.0
reportlab==4.4.10
scikit-learn>=1.3.0
gunicorn==23.0.0
